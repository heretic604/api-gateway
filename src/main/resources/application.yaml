spring:
  application:
    name: api-gateway
  cloud:
    gateway:
        discovery:
          locator:
            enabled: true
            lower-case-service-id: true

eureka:
  instance:
    preferIpAddress: true

server:
  port: 80

logging:
  level:
    ROOT: INFO